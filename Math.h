#pragma once

#include "Vector3.h"

float pointToPlaneDistance(const Vector3& pointOnPlane, const Vector3& point, const Vector3& planeNormal);
float pointToPlaneDistance(const Vector3& pointOnPlane, const Vector3& point, const Vector3& planeNormal);